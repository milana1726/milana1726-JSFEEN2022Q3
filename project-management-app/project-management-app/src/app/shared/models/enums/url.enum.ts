export enum UrlEnum {
  baseURL = 'http://localhost:3000/',
  signIn = 'auth/signin',
  signUp = 'auth/signup',
  users = 'users',
  boards = 'boards',
  columns = 'columns',
  tasks = 'tasks',
  file = 'file',
}