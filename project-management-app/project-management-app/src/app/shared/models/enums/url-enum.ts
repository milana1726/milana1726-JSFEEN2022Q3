export enum UrlEnum {
  // baseURL = 'http://localhost:3000/',
  baseURL = 'https://backend-mongodb-production.up.railway.app/',
  signIn = 'auth/signin',
  signUp = 'auth/signup',
  users = 'users',
  boards = 'boards',
  boardsSet = 'boardsSet',
  columns = 'columns',
  tasks = 'tasks',
  file = 'file',
}